node_modules/
dist/
*.log
.DS_Store
.env
.env.local

# regenerate with `npm run generate:data` — deterministic (seeded), not committed
data/generated/

# SQLite db + WAL sidecar files (regenerated by npm run load:data)
data/db/

# agent decision runs (regenerate with npm run agents:baseline, etc.)
data/results/
